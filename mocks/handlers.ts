import { RequestHandler } from 'msw';

// All Airtable MSW handlers were removed when Airtable was replaced by Supabase (2026-03).
// Add new handlers here as needed for integration tests.
export const handlers: RequestHandler[] = [];
