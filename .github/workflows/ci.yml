name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test & Build
    runs-on: ubuntu-latest
    env:
      VITE_AIRTABLE_API_KEY: test_api_key
      VITE_AIRTABLE_BASE_ID: test_base_id

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Run unit tests
        run: npm run test:unit

      - name: Run integration tests
        run: npm run test:integration

      - name: Build
        run: npm run build

  # E2E tests disabled - need to update selectors and fix dev server startup
  # e2e:
  #   name: E2E Tests
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - run: npm run test:e2e
